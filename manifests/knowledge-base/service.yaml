apiVersion: v1
kind: Service
metadata:
  name: archon-agent
  namespace: archon-kb
  labels:
    app: archon-agent
spec:
  selector:
    app: archon-agent
  ports:
    - port: 8080
      targetPort: http
      name: http
