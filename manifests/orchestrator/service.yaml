apiVersion: v1
kind: Service
metadata:
  name: archon-rag
  namespace: archon-inference-orchestrator
  labels:
    app: archon-rag
    app.kubernetes.io/name: archon-rag
    app.kubernetes.io/part-of: archon
    app.kubernetes.io/component: orchestrator
spec:
  type: ClusterIP
  selector:
    app: archon-rag
  ports:
    - port: 8080
      targetPort: http
      name: http
